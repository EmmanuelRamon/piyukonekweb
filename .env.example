# Piyukonek - Environment Variables Template
# Copy this file to .env and fill in your actual values.
# Command: copy .env.example .env  (Windows) or  cp .env.example .env  (Mac/Linux)
# NEVER commit .env to version control - it contains secrets!

# Required: Generate a random 32+ character string for production
# Example: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-change-this-in-production

# Database - Use your hosting provider's MySQL connection string
# Format: mysql+pymysql://username:password@host:port/database_name
DATABASE_URL=mysql+pymysql://root:password@localhost:3306/piyukonek

# Email (Gmail - use App Password, not regular password)
# https://support.google.com/accounts/answer/185833
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password
MAIL_DEFAULT_SENDER=your-email@gmail.com

# Optional: AI Chatbot (Hugging Face)
# Get token at https://huggingface.co/settings/tokens
HF_API_TOKEN=

# Optional: OpenAI (if used)
# Get key at https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Production settings
# Set to false for production, true for local development
FLASK_DEBUG=false
FLASK_ENV=production

# Set to true when using HTTPS (required for production)
# SESSION_COOKIE_SECURE=true when HTTPS is enabled
HTTPS_ENABLED=false
