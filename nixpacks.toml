# Railway/Nixpacks config for Piyukonek Flask app
# Start command: Gunicorn bound to Railway's PORT
[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "gunicorn -b 0.0.0.0:${PORT:-5000} piyukonek.app:app"
